import { FieldResolver } from 'nexus'
import { NexusGenObjects } from 'server/nexus/generated/nexus'

export const n8nWorkflowsResolver: FieldResolver<
  'Query',
  'n8nWorkflows'
> = async () => {
  const n8nWorkflows: NexusGenObjects['N8nWorkflow'][] = [
    {
      key: 'lesson-01-if',
      name: 'Урок 1. Условная логика (If)',
      description:
        'Изучите базовую условную логику в n8n: ручной триггер, узел IF для проверки условий и разветвление потока данных.',
      // markdown
      content: `# Урок 1: Условная логика (If)

## Описание
Этот урок знакомит с базовой условной логикой в n8n. Вы научитесь использовать узел IF для проверки условий и управления потоком выполнения воркфлоу.

## Что изучается
- **Ручной триггер (Manual Trigger)** - запуск воркфлоу вручную
- **Узел IF** - проверка условий и принятие решений
- **Разветвление потока данных** - выполнение разных действий в зависимости от условия

## Задание
Воркфлоу демонстрирует простую проверку числа:
- Если число **больше 10** → выводится "Большое число"
- Если число **меньше или равно 10** → выводится "Маленькое число"

## Как использовать
1. Откройте воркфлоу "Lesson 01: IF Logic"
2. Нажмите кнопку **"Execute Workflow"** для запуска
3. Измените значение в узле "Set Number" и запустите снова
4. Попробуйте создать свои условия

## Практика
Попробуйте модифицировать воркфлоу:
- Измените пороговое значение (вместо 10)
- Добавьте третье условие (например, "Очень большое число" для значений > 100)
- Создайте проверку для отрицательных чисел
`,
    },
  ]

  return n8nWorkflows
}
