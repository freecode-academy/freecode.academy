import styled from 'styled-components'

export const TagViewStyled = styled.section`
  .TableView {
    margin-top: 30px;

    h2 {
      font-size: 1rem;
      font-weight: normal;
    }
  }

  .tag--all-tags-link {
    margin-top: 20px;
    display: inline-block;
  }
`
