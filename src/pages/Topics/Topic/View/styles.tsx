import styled from 'styled-components'

export const TopicViewStyled = styled.section`
  .root: {
    margintop: 15;
    marginbottom: 30;
    & pre: {
      whitespace: pre-line;
    }
  }

  .header: {
    marginbottom: 30;
  }
`
